spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml
    sentinel:
      transport:
        dashboard: localhost:8858
        port: 8820
      eager: true
      datasource:
        kaasadin:
          nacos:
            server-addr: http://localhost:8848
            dataId: "KASSADIN_ADMIN_RULES"
            groupId: "SENTINEL_GROUP"
            rule-type: flow
